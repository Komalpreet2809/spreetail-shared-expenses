# Set on Vercel as an environment variable instead of committing the real URL.
# Falls back to this if VITE_API_URL is not provided at build time.
VITE_API_URL=https://REPLACE-WITH-RENDER-BACKEND.onrender.com/api
